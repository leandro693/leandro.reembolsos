REGISTRO DO "ANTES" — migration 0014 (crédito/saldo). Data: 2026-08-12.

Estado atual (antes de aplicar a 0014), conforme migrations 0000..0012:
- public.creditos_operador: NÃO EXISTE.
- public.empresa_usuarios: NÃO tem a coluna modo_lancamento
  (colunas: id, empresa_id, usuario_id, papel, setor_id, ativo, criado_em).
- Funções lancar_credito / set_modo_operador / remover_credito / saldo_operador: NÃO EXISTEM.

Confirmação read-only a rodar IMEDIATAMENTE ANTES de aplicar (parte do ritual):
  select json_build_object(
    'creditos_operador_existe', to_regclass('public.creditos_operador') is not null,
    'modo_lancamento_existe', exists (select 1 from information_schema.columns
       where table_schema='public' and table_name='empresa_usuarios' and column_name='modo_lancamento')
  );
  -- Esperado ANTES: { creditos_operador_existe:false, modo_lancamento_existe:false }

ROLLBACK: supabase/auditoria/rollback-0014.sql (drop functions/table/column).

Observação: o FRONT foi escrito RESILIENTE — detecta se a coluna/tabela existem
(flag creditoDisponivel); enquanto a 0014 não é aplicada, o app funciona idêntico ao
modo despesa atual (sem UI de crédito, filtros da decisão D no-op).
